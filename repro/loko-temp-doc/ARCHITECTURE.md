# Architecture

Top-level system overview. Stable reference doc.

- **peer/** — peer discovery + registry
- **router/** — message routing across peers
- **store/** — append-only event log

This doc has no `covers:` frontmatter — it's narrative architecture, not
tied to specific code paths. Included in the fixture as an "untagged but
manually pinned" example to prove default-keep still applies to
non-temp untagged docs.
