---
covers:
  - src/module-bar/
---

# Module Bar Guide

Reference for `module-bar`. Covers paths are *active* (other contributors
keep editing src/module-bar/), but no active session has referenced this
doc. Demonstrates Fix B: code-freshness ≠ doc-freshness; session-mention
is the better proxy.

Under v0.4.8 this doc was kept indefinitely (reactive: covers paths
touched → keep). Under Fix B + Fix C this doc is auto-removed once
`presence=established` AND `mentioned=no`.
