# Freshly Added

Untagged narrative doc added to LOKO in the most recent wrap (presence
=recent or =new in the helper output, depending on commit timing). No
session has had a chance to mention it yet.

Demonstrates Fix C's grace-period: even if Claude judges this off-theme
for the next session, auto-mode should NOT remove it — it gets one
wrap of advisory listing first so the user can pin it if they want.
